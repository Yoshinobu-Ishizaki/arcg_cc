- Output should be 2 types. RTX or DXF. For rtx, it must obey our special RTX format with extension .rtx. Read @doc/rtx_spec.md for RTX format.
- When saving params and result, default filename's name should be same as input file. If the opened file name is "untitled.csv", params file should be "untitled.yaml", output shoud be "untitled.{rtx|dxf}.
- Check if the whole source code obeys the algorithm instructed in @doc/arcg_algorithm.md . If not, change codes to satisfy it.

